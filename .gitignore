# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files - ignore local and secret env files but allow example/template files
# Ignore per-environment local overrides and developer-local secrets
.env.local
.env.*.local
# Optionally ignore a plain .env file if you keep secrets there; leave commented so teams can opt-in
# Ignore plain .env (local secrets)
.env
# Allow committed example/template env files (do NOT remove this line)
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/generated/prisma

/generated/prisma

/generated/prisma

.vercel
.env*.local

/generated/prisma

/generated/prisma
